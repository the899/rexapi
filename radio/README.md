
# 250918

1. 什么是 PWA
2. 给出一个最简单的 PWA 完整代码
3. 可以将这个文件部署在 Cloudflare 的 pages 里吗？
4. 如果已经在 cf 上部署了某个 pages ，现在将这个 PWA 放到其子目录可以吗？
5. 我想做一个 PWA 的应用，名字叫 Radio，所以目录应该修改成 radio。我计划逐步去实现它，我们约定一个名称叫 project_radio ：当我提及这个名字时，表明我想重新对这个话题进行行更深入的交流，在交流过程中，请全程使用中文进行回复。
6. 使用更为现代的深色模式。
7. 哪个在线站点可以帮忙设计 icon
8. 更推荐哪个 ？
9. 功能规划：用于播放下述链接所示的在线广播节目(4地址)。
10. 不需要显示一个 有播放/暂停按钮的独立控件，点击卡牌即开始播放，再次点击时停止。
11. 不需要显示标题，也不需要显示当前播放的广播名称，不需要 音量控制，不需要在卡牌上添加广播名称，不需要推送通知，去掉‘Radio PWA’以及期占位。需要保存当前选中的频道，一定需要后台音频播放的支持。使用以下 9 个频道(9地址)
12. 在海报上显示动态效果，以表明以下状态：
    1. 正在连接：表明正在与频道的网址进行连接；
    2. 正在播放：表明正在进行广播内容的播放；
    3. 播放停止：表明已停止播放；4. 无法连接：表明无与频道建立连接
13. 状态切换问题：
    1. 当前频道未选中时，单击此频道时，表示选中、连接频道地址、开始播放；
    2. 频道如果连接不成功，3秒后再次尝试连接，直至连接成功开始播放；
    3. 频道正在播放中：点击频道，在播放和停止播放两者间切换；
14. 暂停 时 显示通用的 双竖线 暂停图标
15. 首次加载时：取出上次使用的频道并进行播放。
16. 修改暂停：频道不要变暗和变小，此频道还是处于选中的状态。
17. 修改 index.html：
    1. 首次加载此站点时，未有任何频道被选中，所有频道处理未选中状态；
    2. 使用之后，刷新网页或再次打开浏览器浏览此站点：应该最后使用的频道被选中，并开始加载连接并播放。
18. 取消 9 个频道的约定，频道的数量取决于 config.json
19. 取消海报文件，画出与海报同尺寸的正方形，园角与苹果的园角类似。按钮上显示频道的名称。
20. 修改按钮未选中时的颜色，，也能看见它是个按钮。字体增大到原大小的 150%
21. 修改按钮未选中时的颜色：未选中时按钮颜色与背景相同。
22. 字体修改为 1.5rem ，移动端缩小。
23. 频道正在播放中时，刷新页面后：频道显示了暂停图标，没有恢复到播放的状态。
24. 问题并未解决：当刷新时，应该进行如下流程
    1. 取出保存中的频道，显示在界面；
    2. 尝试连接频道：显示重试连接的动画，直至连接成功开始播放；
    3. 如果成功播放：显示播放中的动画。
25. 刷新后仍然是暂停状态。
26. macOS 下的 Chrome 在刷新后，可以正常进入播放状态了。macOS 下的 Safari 在刷新后，图标变为了播放的图标，但是没有播放音频。需要再次修改。
27. Safari（macOS/iOS）对自动播放有严格限制，player.play() 在无用户交互时可能被阻止，导致 promise 失败，但未触发 catch 块或 error 事件。如果确认 safari 有此限制行为 ？
28. 增加处理时间 由 3 秒增加到 5 秒，给网络处理音频的时间。
29. 在 macOS 下，添加到程序坞：启动 PWA 可以自动播放。但是 PWA 应用如果点击左上角的关闭图标，再次启动它时，出现了：
【RexRadio无法打开页面，RexRadio无法打开页面“<https://a.a1b2.cc/radio/>”。错误是："Response served by service worker has redirections"(WebKitInternal:O)，重新载入RexRadio】。原因是什么？
30. 55去掉与 添加 PWA 的所有引导提示，由使用者自行决定添加。
31. 修改【无法自动播放，请点击提示或频道开始播放】的提示：为 【无法自动播放，请点击频道开始播放】，显示 3 秒后消失。频道的图标修改为暂停的图标。当前播放中的图标没有动画的效果，恢复为动画的效果。
32. 将所有频道的默认图标（radio-card::after）设置为 暂停图标（paused 状态的图标：|| 样式）。这个不对。是将当前频道的图标修改为暂停的图标。不存在所有频道的图标这个说法，只有当前频道才会有图标的显示。
33. 当前音频还在播放中，图标已经变成了红色的错误图标，然后重新请求频道内容，明显听到音频有重新开始播放。是否对状态的检测有问题 ？
34. 优化：* 添加“重置应用”按钮清除 localStorage 和 SW 缓存：这里的代码要添加到哪个文件？
35. 现在开始调试在 iOS 上的 PWA 界面：safari 打开 <https://a.a1b2.cc/radio> 后，分享 - 添加到主屏幕，目前功能正常，需要修改一下界面上的颜色。这个单页面是深色模式，当横屏显示时，左边和右边出现了蓝色的竖边。当竖屏显示时，上面出现蓝色的竖边。这些蓝色的边是什么 ？在 macOS 上添加到程序坞后，运行它窗口的标题栏也是蓝色的，与深色的主题非常不相符。取消显示这些蓝色的边框，如果不能取消：将颜色修改为与背景相同的深色。
36. 颜色调整得非常好。现在调整按钮区域的位置，无论何时，它位应该上下左右居中显示。如果窗口大小无法容纳它们：设置一个最小的窗口宽度和高度，当小于这个数字时，自动放大窗口。文字最小为 0.5rem，移动端的文字最小为 0.4rem。相应的按钮的尺寸也按此比例有一个最小值。当手机在横向和竖向变化时，按钮区域也会自动居中显示。
37. 移除播放图标，播放中不显示图标。根据 Apple 的设计规范：为卡片区域添加安全区，与浏览器的边框保留适当的间距。
38. 重置应用：清除 localStorage 和 SW 缓存，重新加载 config.json。在卡片队列的末尾添加一张卡牌：将重置应用的功能添加在此卡牌上，重置应用需要确认。
39. 现在开始调试在安卓平板(Redmi Pad SE 11，Android 13)上的 PWA 界面：edge 打开 <https://a.a1b2.cc/radio> 后，分享 - 添加到手机，目前功能正常，需要修改界面颜色。这个单页面是深色模式，当横屏显示时，左边和右边出现了蓝色的竖边。当竖屏显示时，上面下面出现蓝色的竖边。这些蓝色的边是什么 ？它与深色的主题非常不相符。取消显示这些蓝色的边框，如果不能取消：将颜色修改为与背景相同的深色。修改时，不要涉及到 iOS 相关的设置，目前在 iOS 上一切正常。
40. 删除无关的代码、无关的配置，删除用于调试的日志记录。拆分 index.html，将 css js 拆分到 radio.css radio.js 中。
41. 横向不允许滚动，仅允许竖向滚动。当选中某个卡牌，自动竖向滚动：尽可能让当前行处于屏幕的中间位置。
42. 测试用例
    1. MacBook Air M2, macOS 15.2：Safari, PWA；
    2. iPhone13, iOS 18.3.1, 横屏、竖屏：Safari, PWA;
    3. Redmi Pad SE 11，Android 13, 横屏、竖屏: Edge, PWA;
    4. Redmi 13C, 横屏、竖屏：Edge, PWA。
43. 显示当前项目文件最新版本的内容。
44. the end
